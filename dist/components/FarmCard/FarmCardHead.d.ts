import React from "react";
interface FarmCardHeadProps {
    isSoon?: boolean;
    isNew?: boolean;
    children: Array<React.ReactElement>;
}
declare const FarmCardHead: React.FC<FarmCardHeadProps>;
export default FarmCardHead;
